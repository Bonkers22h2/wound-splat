# Wound tissue segmentation — training environment (tissue-venv).
#
# DELIBERATELY SEPARATE from backend/requirements.txt. The backend runs the
# working reconstruction pipeline; installing these packages into it risks
# breaking that. Never merge the two.
#
# Install from the repository root:
#   py -3.11 -m venv tissue-venv
#   tissue-venv/Scripts/python.exe -m pip install -r tissue/requirements.txt \
#       --extra-index-url https://download.pytorch.org/whl/cu126
#
# Target hardware: NVIDIA RTX 4050 Laptop, 6 GB VRAM, driver 610.62.

# --- deep learning ---
# cu126 wheels are not on PyPI; they come from the extra index URL above.
# Version matched to backend/requirements.txt so both use one CUDA runtime.
torch==2.12.0+cu126
torchvision==0.27.0+cu126

# --- model architecture ---
# Provides the mit_b3 (Mix Transformer) encoder and scSE decoder attention
# used by the DFUTissueSegNet architecture, plus the Dice loss.
segmentation-models-pytorch==0.5.0
timm==1.0.29

# --- data handling ---
numpy==2.4.6
pillow==12.2.0

# --- testing ---
pytest==9.1.1
