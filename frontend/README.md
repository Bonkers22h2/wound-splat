# Wound-Splat Frontend

Next.js UI for Wound-Splat. See the [root README](../README.md) for setup, architecture, and usage.

```bash
npm install
npm run dev   # http://localhost:3000 (expects the backend on :8000)
```
