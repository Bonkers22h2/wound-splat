# Wound-Splat backend — direct dependencies only (transitive deps resolved by pip).
# Install from the backend/ directory (the CUDA extension paths below are relative):
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cu126
# The +cu126 torch wheels are NOT on PyPI — install PyTorch first per README Step 3.

# --- API server ---
fastapi==0.136.3
uvicorn==0.49.0
python-multipart==0.0.32   # multipart form uploads (video + reference-object fields)
SQLAlchemy==2.0.50
pydantic==2.13.4

# --- PDF reports (generate_report.py) ---
reportlab==4.5.1

# --- 3D pipeline: training, segmentation, measurement ---
torch==2.12.0+cu126        # from https://download.pytorch.org/whl/cu126
torchvision==0.27.0+cu126  # from https://download.pytorch.org/whl/cu126
numpy==2.4.6
scipy==1.17.1
opencv-python==4.13.0.92
open3d==0.19.0             # pulls dash/flask/ipywidgets/nbformat as its own deps
plyfile==1.1.4
pillow==12.2.0
tqdm==4.68.2

# --- Local CUDA extensions — relative paths (run pip from the backend/ directory).
# Built with the MSVC toolchain; see README "Compile the 3DGS CUDA extensions".
../gaussian-splatting/submodules/diff-gaussian-rasterization
../gaussian-splatting/submodules/simple-knn
ninja==1.13.0              # speeds up the CUDA extension builds

# --- Optional distributed queue (app/tasks/pipeline.py + celery_worker.py).
# The default pipeline (pipeline_direct.py) does not need these.
celery==5.6.3
redis==8.0.0
